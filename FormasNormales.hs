module FormasNormales (fnn, fnc, fnd) where

import FormulasProposicionales
import Operadores

-- Función que dada una fórmula f, regresa una fórmula equivalente a f
-- en su Forma Normal Negativa.
fnn :: Prop -> Prop
fnn = error "D:"

-- Función que dada una fórmula f, regresa una fórmula equivalente a f
-- en su Forma Normal Conjuntiva.
fnc :: Prop -> Prop
fnc = error "D:"

-- Función que dada una fórmula f, regresa una fórmula equivalente a f
-- en su Forma Normal Disyuntiva.
fnd :: Prop -> Prop
fnd = error "D:"
